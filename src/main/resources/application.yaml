spring:
  datasource:
    url: jdbc:mysql://localhost:3306/salon_queue
    username: root
    password: atharva@07raje
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect